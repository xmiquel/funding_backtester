name: "funding_backtester"
version: "1.0.0"
config-version: 2

profile: "tick_data"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  funding_backtester:
    staging:
      +materialized: view
    marts:
      +materialized: table
